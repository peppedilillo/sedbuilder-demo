[project]
name = "sedbuilder-demo"
version = "0.1.0"
description = "A demo for the SED Builder python client."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=3.0.3",
    "seaborn>=0.13.2",
    "ssdc-sedbuilder>=2.0.0",
]
